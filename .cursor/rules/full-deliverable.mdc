---
description: 全案汇编/完整方案文档的固定章节、各章写法、公文语体与附录要求；适用任意场景扩写。
globs:
  - scenes.json
  - "**/briefing-app.js"
  - "**/patch-full-deliverable*.mjs"
  - ".cursor/rules/*"
alwaysApply: false
---

# 全案汇编模块生成规则

当用户要求生成任何场景的 **「全案汇编」** 或 **「完整方案文档」** 时，必须严格遵循以下 **架构与风格**。  
工程内落地：`scenes.json` 的 `briefingDepthTemplates.full`（含 `cover`、`preface*`、`disclaimerBox`、`bodyChapters`、`appendices`）及 `renderHrDeliverableShell` 渲染逻辑。

## 固定章节结构（一级标题）

正文依次出现（编号与措辞保持一致）：

- 前言与编制说明  
- 一、场景定义与痛点分析  
- 二、核心价值主张  
- 三、任务规划与作业流程  
- 四、装备选型与地面保障  
- 五、推荐编成与部署方案  
- 六、安全与合规底线  
- 七、附录  

章节之间须有 **承接语句**，避免硬性跳题。

## 各章节内容要求

### 前言与编制说明

- **编制背景**、**适用场景**、**版本号**（如 V1.0）、**编制单位** 等栏位信息完整；可与封面元数据对应。
- 末尾必须设 **声明框**，表述等价于：**本方案为演示叙事框架，定量指标需依据客户实际战训数据、厂方最新规格及空域批文调整；法规条文以正式公布文本为准。**

### 一、场景定义与痛点分析

- 保留与本场景相关的 **火灾/事故类型分类**（叙述化，非堆砌条目）。
- **传统手段痛点** 凝练为 **一段或多段完整叙述**；原 **对比表格** 移至 **附录**，正文仅一句指向附录。

### 二、核心价值主张

- 提炼 **3～4 点**；每条 **标题式短语 + 一句量化或对策叙事**。
- 每条 **定量或对标数据** 须有 **脚注式本地替换提示**（如 `[²]`、`[³]` 或统一脚注符号），说明以战训、批文、厂方规格为准。

### 三、任务规划与作业流程

- 按 **时间线/阶段** 叙述（如「侦察—打击—看护」）；阶段表在正文 **一句话引用附录**，**完整表放附录**。
- **多机协同、热成像运用等技巧** 必须 **融入各阶段叙述**，**禁止** 单独成节裸列技巧清单。

### 四、装备选型与地面保障

- **删除** 正文中的纯表格列装；选型理由写成 **连贯段落**。
- **地面能源、充电/换电/系留策略** 并入正文叙述；**详细参数表** 放附录。

### 五、推荐编成与部署方案

- **最小作战编成** 用 **列表**（有序/无序均可），**不用表格**。
- **禁止**「数据完善中」等开发占位；不确定信息统一 **〔待补充〕**（与项目内其它档位的「〔待采购确认〕」并存时，全案正文优先按全案约定使用 **〔待补充〕**）。

### 六、安全与合规底线

- **飞行安全** 与 **作业安全要点** 合并叙述，层次清晰。
- **法规快表** 以 **附录引用** 为主，正文点出核心义务即可。

### 七、附录

**必须** 包含（可对应多附录或小节）：

- **装备选型对比**（或选型与地面保障原始表）  
- **法规索引快表**（文件/施行时点 + 要点；注明以正式 PDF/公布文本为准）  
- **边界声明全文**  
- **厂商联系渠道**（以官网最新公布为准；缺省处 **〔待补充〕**）

## 语言与格式规范

- **政府公文** 偏正式书面语；关键价值点可用 **排比** 增强力度，避免口号化。
- 技术参数 **不再使用括号夹注**；统一改为 **段末脚注式说明**（如 ¹ ² 或 `[¹]`，全文体例一致）。
- **插图位** 在正文保留建议标记，如：`【插图位】……示意图`。
- 向用户 **直接交付文稿** 时，输出 **完整 Markdown**（`#` / `##` 层级与上述一级标题对齐）；若写入仓库 JSON，则保持字段叙事与上述要求等价，由现有 HTML 壳层渲染。

## 与生成器数据的对应（核对用）

| 规则板块     | 典型字段 |
|-------------|----------|
| 封面四项     | `full.cover`：`organization`、`applicableScene`、`version`、`dateText` |
| 前言+声明   | `prefaceTitle`、`prefaceParagraphs`、`disclaimerBox` |
| 正文章节    | `bodyChapters[]`：`chapterNo`、`title`、`intro`、`sections[]`（`heading`、`body`、`paragraphs`） |
| 附录        | `appendices[]`：`paragraphs`、`lawTable`、`detailTable` 等 |

## 提示词模板（聊天中可粘贴）

请按仓库 `.cursor/rules/full-deliverable.mdc` 的 **全案汇编** 规则，为场景【　　　】生成一份完整方案：固定九章结构、公文语体、声明框与附录四件套齐全；正文禁用开发占位；定量均带脚注式替换说明；输出 **Markdown 全文**（或写入 `briefingDepthTemplates.full` 的 JSON 结构说明）。
