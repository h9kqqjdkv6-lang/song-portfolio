---
description: 方案生成器三档输出（决策速览一页纸、技术深化解析、全案汇编报送稿）定位与禁止项
alwaysApply: true
---

# 方案生成器 · 三档文档（严格执行）

编辑 `scenes.json` 的 `briefingDepthTemplates`、`js/generator/briefing-app.js` 的 `renderHighRise*` 及导出文案时，必须按 **文档深度** 遵守下列约束，禁止混档。

## 模块一：决策速览（一页纸）

**受众**：非技术决策者（局长、分管领导）。

**硬性要求**：全文 **不超过 800 字**（含标点）；**固定板块顺序**（圆圈序号随是否含「关键节奏」辅段自动顺延）：
① **场景核心矛盾**（一段）  
② **我方方案**（`solutionSummary`，一句话收束从够不到到精准打击，可含约 X 分钟级锚点；与下文「关键节奏」二选一或并存）  
③ **核心能力**（**3 条**，每条 **不超过 30 字**）  
④ 可选：**关键节奏**（`timeWindowNarrative`，「从…到…」叙事；若不写则不占序号）  
⑤ **最小作战编成**：**类型 + 任务分工 + 数量**，**禁止** 写型号全称、机名、载荷名  
⑥ **安全与合规底线**（一句话）  
⑦ **需贵局明确的下一步**（1～2 条）  
**页脚**：一条注释，标明定量以本地战训为准、法规以正式公布文本为准（`overview.footerNote`）。

**禁止**：一切技术参数；括号说明；表格；任何具体机型号与载荷型号；除上述页脚外不再堆叠「待核实」长文。

**文风**：书面、有力；可用排比；避免口号化与空泛形容词堆砌。

## 模块二：技术深化（解析体例）

**受众**：业务科室（作训、信通、装备）。

**叙事主轴**：**问题 → 技术逻辑 → 实现方式 → 操作要点**，全文连贯，**不得**手册式裸列参数。

**结构**：**侦察、灭火、看护** 三节；每节 **先叙事**，再配 **必要简化表**，最后 **实战技巧**。允许写机型与载荷名称；**定量数据** 必须标注 **「需客户战训数据替换」** 或 **「以厂方最新规格为准」**。

**文末**：附 **关键术语解释**（如系留、RTK、高增益、熔断返航等），便于非无人机专业人员阅读。

## 模块三：全案汇编（报送稿）

**受众**：采购、实施、第三方评审。

**细则**：章节结构、各章写法、声明框、附录四件套、脚注与 **〔待补充〕** 等，以 **`.cursor/rules/full-deliverable.mdc`** 为准（固定「前言与编制说明」至「七、附录」共八个一级板块；承接语、公文语体、技巧融入阶段叙述等）。

**工程占位**：生成器底层仍可能使用 **〔待采购确认〕**；**全案正文**撰写时优先 **〔待补充〕**，与 `full-deliverable.mdc` 一致。

**合规**：法规快表须列 **条文/时点与要点**，并注明 **「以正式公布文本为准」**。

## 验收

- 三档并排对比时，一眼能区分 **受众与信息粒度**。
- **决策速览** 中不得出现 **米每秒、公斤、产品型号** 等字样。
